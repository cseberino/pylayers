(macro for args
       (if (and (>= (len args) 2) (atom (first args)))
           (append (append (append (noeval (if)) (append (append (noeval (=))
           (second args)) ())) True) (append (extend (append (noeval (block))
           (list (noeval def) (first args) (append (noeval (first)) (second
           args)))) (rest (rest args))) (extend (list (noeval for) (first args)
           (append (noeval (rest)) (second args))) (rest (rest args)))))
           (noeval)))
