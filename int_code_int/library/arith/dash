(def -
     (func args
           (if (= (len args) 1)
               (negate (first args))
               (if (= (len args) 2)
                   (+ (first args) (- (first (rest args))))
                   (noeval)))))
